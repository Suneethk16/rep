# Base URL of the FastAPI backend (no trailing slash)
# Leave empty to use the Vite dev-server proxy to http://localhost:8000
VITE_API_BASE_URL=

# Used only by vite dev-server proxy (local dev)
VITE_DEV_API_TARGET=http://localhost:8000
